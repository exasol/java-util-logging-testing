language: java

matrix:
  include:
    - jdk: "openjdk9"

addons:
  sonarcloud:
    organization: exasol
 
script:
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar

cache:
  directories:
  - $HOME/.m2
